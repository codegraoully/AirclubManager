{% extends 'base.html.twig' %}

{% block title %}Brevet{% endblock %}

{% block body %}
    <h1>Brevet</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Identifiant</th>
            <td>{{ brevet.id }}</td>
        </tr>
        <tr>
            <th>Libelle</th>
            <td>{{ brevet.libelle }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('brevet_index') }}" class="btn btn-secondary">Retour</a>

    <a href="{{ path('brevet_edit', {'id': brevet.id}) }}" class="btn btn-secondary">Modifier</a>

    {{ include('brevet/_delete_form.html.twig') }}
{% endblock %}
